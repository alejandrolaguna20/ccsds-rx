ccsds-rx
Real-time telemetry engine for decoding CCSDS Space Packet Protocol frames from live satellite observations.
Ingests raw hex frames from the SatNOGS network, parses the binary structure at the bit level, and renders decoded telemetry in a terminal interface.
